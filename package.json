{
  "name": "taigi_typing",
  "version": "1.0.0",
  "private": true,
  "description": "台語文拍字練習網頁",
  "license": "CC0-1.0",
  "scripts": {
    "patch:kesi": "node scripts/patch-kesi.mjs",
    "build:kesi": "npm run patch:kesi && esbuild node_modules/@kemdict/kesi/dist/index.js --bundle --format=iife --global-name=Kesi --outfile=kesi.bundle.js --platform=browser --minify"
  },
  "dependencies": {
    "@kemdict/kesi": "^0.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
